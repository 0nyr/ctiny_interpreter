int foo(int a) { 
    while (a > 0) { 
        a = a - 1; 
    } 
    return a; 
}

int main() {
    int a;
    int y;
    a = foo(42);
    y = 10; while (y > 0) { 
        if (a < 100) { 
            a = a + y; 
        } 
        y = y - 1; 
    } 
    return a; 
}